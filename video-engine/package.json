{
  "name": "nuhoot-ffmux",
  "version": "1.0.0",
  "description": "FFmux — REST API over FFmpeg for zero-marginal-cost video generation. JSON timeline → MP4 Reels.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "fluent-ffmpeg": "^2.1.3",
    "ffmpeg-static": "^5.2.0",
    "uuid": "^9.0.0",
    "cors": "^2.8.5"
  }
}
